{
    "aws_account_info": "SELECT * FROM {{connection_id}}.aws_account",
    "aws_":"select instance_id as bhragav_id, instance_state, launch_time, state_transition_time from {{connection_id}}.aws_ec2_instance where instance_state = 'stopped' and state_transition_time <= (current_date - interval '60' day);"
}